// Método que permite extrair elementos de subconjunto de uma array e adicioná-los à nova array. 

var colors = ['red','green','blue','yellow','purple'];
var rgb = colors.slice(0,4);
console.log(rgb);